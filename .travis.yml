language: minimal

services: docker

env:
  - DOCKER_COMPOSE_VERSION=1.23.2

before_install:
  - docker -v && docker-compose -v
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - curl -LO https://storage.googleapis.com/kubernetes-release/release/$(curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt)/bin/linux/amd64/kubectl
  - chmod +x ./kubectl
  - sudo mv ./kubectl /usr/local/bin/kubectl
  - mkdir ${HOME}/.kube
  - cp udacity-c3-deployment/k8s/udagram-kubeconfig ${HOME}/.kube/config
  - kubectl config set jrc294.k8s.local.certificate-authority-data "$KUBE_CLUSTER_CERTIFICATE"
  - echo $KUBE_CLUSTER_CERTIFICATE




install:
  - echo 'Hello Udacity..3.'
  - pwd
#  - docker-compose -f udacity-c3-deployment/docker/docker-compose-build.yaml build --parallel
#  - kubectl apply -f reverseproxy-deployment.yaml
#  - docker login -u "jrc294" -p "Command1234$" docker.io
#  - docker push jrc294/udacity-restapi-feed
#  - docker push jrc294/udacity-restapi-user
#  - docker push jrc294/reverseproxy
#  - docker push jrc294/udacity-frontend:local
